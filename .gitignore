node_modules/
dist/
.astro/
.env
.env.production
.env.local
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# bench scratch assets (big AI-gen originals; optimized copies live in public/plate)
bench/assets/

# large raw plate renders (production copies live in src/assets/ui)
bench/store-plate/*.png
bench/showcase/*.png

# OpenAI key for gen-*.sh (never commit)
.openai_key.local

# crane buy-card pipeline scratch — keep only the irreplaceable one-render sources
tools/_crane-candidates/*
!tools/_crane-candidates/master-installed-orig.png
!tools/_crane-candidates/wear-installed-orig.png

# GAS drive-in car pipeline scratch (gen-gas-car.sh) — 2528px gpt-image-1
# candidates; the picked+optimized copies live in src/assets/plugins/. Same rule
# as the crane dir: add a `!` line for any render worth keeping in git.
tools/_gas-car-candidates/*

revedit.overrides.json

# Editor / agent local config
.claude/

# Playwright
test-results/
playwright-report/
